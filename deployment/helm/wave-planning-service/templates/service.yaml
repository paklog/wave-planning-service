apiVersion: v1
kind: Service
metadata:
  name: {{ include "wave-planning-service.fullname" . }}
  namespace: {{ .Values.namespace | default "paklog" }}
  labels:
    {{- include "wave-planning-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.service.managementPort }}
    - port: {{ .Values.service.managementPort }}
      targetPort: management
      protocol: TCP
      name: management
    {{- end }}
  selector:
    {{- include "wave-planning-service.selectorLabels" . | nindent 4 }}
